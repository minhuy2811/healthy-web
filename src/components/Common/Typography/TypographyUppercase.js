import { styled } from "@mui/material/styles";
import Typography from "@mui/material/Typography";

const TypographyUppercase = styled(Typography)(() => ({
  textTransform: "uppercase",
}));

export default TypographyUppercase;
